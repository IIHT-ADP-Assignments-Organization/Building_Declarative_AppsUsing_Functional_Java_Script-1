<Script>

/*
 1. Implement a custom version of Redux & Setup the reducer to handle a specific action to state & show how does our
    application gets updated & dispatch the actions to initiate change.

 2. Perform middleware creates a function acts as a go-between from app to store, 
    calculate initial state+actions= current state & replay actions.

 3. Load the slides into redux(-ish) store, create a UI with controls & events 
    through Virtual-DOM & trigger event actions to update state purely.

 4. Create a single object store in redux & Setup the reducer to handle a specific action,
    get updates & dispatch the actions. create a function acts as a go-between 
    from app to store & calculate current state && trigger event actions to update state purely.


*/

</Script>